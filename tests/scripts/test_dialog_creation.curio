# Test Dialog Creation Script
# This script tests if the dialog can be created without errors

print("=== Test Dialog Creation Script ===")

# Test 1: Check if we can create a project dialog
print("1. Testing dialog creation...")

# Test 2: Trigger New Project dialog (should create dialog but not wait for user)
print("2. Triggering New Project dialog...")
trigger_menu("Project", "New Project")

print("3. Dialog creation test completed")
print("   If no errors occurred, the dialog implementation is working")
print("   The dialog should have appeared briefly but closed automatically")

print("\n=== Test completed ===")
exit(0)

